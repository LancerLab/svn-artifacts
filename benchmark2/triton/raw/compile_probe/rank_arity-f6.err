RESULT run=crash out=none
EXC CompilationError: at 3:8:
def f_expand(x_ptr, out_ptr):
    a = tl.load(x_ptr + tl.arange(0, 256))
    b = tl.expand_dims(a, axis=2)
        ^
invalid axis 2. Expected -2 <= axis < 2
