#include "common.hpp"
__co__ auto r5dyn(f32 [N,4096,1,1,1] input) {
  f32 [N,1,1,1,4096] output;
  parallel p by 1 : block {
    shared f32 [N,1,1,1,4096] tile;
    parallel q by 32 : thread {
      dma.transp<0,2,3,4,1> input => tile;
      dma.copy tile => output;
    }
  }
  return output;
}
