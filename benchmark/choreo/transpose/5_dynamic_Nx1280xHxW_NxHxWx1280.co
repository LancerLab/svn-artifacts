#include "choreo.h"
#include "common.h"

#ifdef __STATIC_SHAPE__
#define batch_size 32
#define height 28
#define width 28
#endif

__co__ auto TRANSPOSE(f32 [batch_size, 1280, height, width] i) {
  f32 [i.span(0), i.span(2), i.span(3), i.span(1)] o;
  parallel p by batch_size : block {
    foreach {y, z, w} in [1280, height, width]
      o.at(p, z, w, y) = i.at(p, y, z, w);
  }
  return o;
}

int main() {
  test4("TRANSPOSE", TRANSPOSE, {32, 1280, 28, 28}, {0, 2, 3, 1});
}
