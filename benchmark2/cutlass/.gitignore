# cutlass lane — generated / build artifacts
raw/
*.o
*.cubin
*.ptx
__pycache__/
# mutant sources are regenerated from the mutation table
mutants/
# raw record streams are COMMITTED (small, ~4KB each) so a fresh clone can
# reproduce results/cutlass/stats.json without a GPU; see collect.py.
# lane-local results dir; canonical output is benchmark2/results/cutlass/
results/
