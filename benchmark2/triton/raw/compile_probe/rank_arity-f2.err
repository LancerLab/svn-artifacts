RESULT run=crash out=none
EXC CompilationError: at 4:8:
def f_permute_dup(x_ptr, out_ptr):
    a = tl.load(x_ptr + tl.arange(0, 256))
    a = tl.reshape(a, (16, 16))
    b = tl.permute(a, (0, 0))
        ^
permute dims must be a permutation of 0, 1, ..., n-1, but were ['0', '0']
